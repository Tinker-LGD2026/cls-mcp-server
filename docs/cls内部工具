cls_search_log
搜索CLS日志内容 在指定日志主题和时间范围内搜索日志内容，支持复杂查询语法和统计分析。 该函数会自动检查日志主题的生命周期，避免查询超出保留期的日志。 同时提供日志分布统计和详细内容展示。 query参数使用CQL（Cloud Query Language）语法，这是腾讯云CLS的专用查询语言。 重要：如果选用了cls_generate_cql工具，务必先使用 cls_generate_cql 生成 CQL 语法！ cls_generate_cql 是专业的CQL生成专家，拥有丰富的实战经验和深厚的技术积累，具备以下核心优势： 自动适配日志主题索引配置，字段名称100%准确 严格遵循CQL语法规范，生成的语句保证可执行 查询性能经过优化，执行效率高，响应速度快 支持从简单过滤到复杂聚合的所有查询场景 警告：如果不使用 cls_generate_cql 工具生成CQL，一定会出错误； CQL语法说明： 1. 全文检索： - 直接输入关键词进行全文搜索，例如：error - 多个关键词用空格分隔，默认为OR关系，例如：error warning 2. 键值检索： - 格式：key:value，例如：level:ERROR、status:404 - 字段名和字段值必须用半角冒号(:)连接 - 字段值支持数字、字符串等类型 3. 短语检索： - 使用双引号("")包裹短语，例如：name:"john Smith" - 适用于包含空格或特殊字符的字段值 4. 模糊检索： - 使用通配符 * 匹配零个或多个字符，例如：host:www.test*.com - 使用通配符 ? 匹配单个字符，例如：status:40? 5. 数值比较： - 支持 >（大于）、>=（大于等于）、<（小于）、<=（小于等于）、=（等于） - 例如：status:>400、response_time:>=1000、age:<18 6. 范围检索： - 使用比较运算符组合，例如：status:>=400 AND status:<500 7. 逻辑运算符： - AND：与运算，例如：level:ERROR AND pid:1234 - OR：或运算，例如：level:ERROR OR level:WARNING - NOT：非运算，例如：NOT level:DEBUG - 支持括号组合，例如：(level:ERROR OR level:WARNING) AND pid:1234 8. SQL分析语句（使用 | 管道符）： - 聚合统计：* | SELECT count(*) AS total - 分组统计：* | SELECT count(*) AS cnt, level GROUP BY level - 排序限制：* | SELECT count(*) AS cnt, host GROUP BY host ORDER BY cnt DESC LIMIT 10 - 条件过滤：* | SELECT * WHERE response_time > 1000 CQL查询示例： - * : 查询所有日志 - error : 全文搜索包含error的日志 - level:ERROR : 查询level字段为ERROR的日志 - status:404 : 查询status字段为404的日志 - name:"john Smith" : 查询name字段为"john Smith"的日志 - host:www.test*.com : 模糊查询host字段以www.test开头.com结尾的日志 - status:>400 : 查询status大于400的日志 - status:>=400 AND status:<500 : 查询4xx状态码的日志 - level:ERROR AND pid:1234 : 查询ERROR级别且pid为1234的日志 - level:ERROR OR level:WARNING : 查询ERROR或WARNING级别的日志 - NOT level:DEBUG : 排除DEBUG级别的日志 - * | SELECT count(*) AS cnt, level GROUP BY level : 统计各级别日志数量 - * | SELECT avg(response_time) AS avg_time, api GROUP BY api : 计算每个API的平均响应时间

cls_describe_topics
获取CLS日志主题列表 查询指定地域的日志主题列表，支持过滤和分页。 可以根据主题名称、ID 等条件进行过滤。

cls_describe_alarms
获取CLS告警策略列表 查询指定地域的告警策略列表，支持过滤和分页。 可以根据告警策略的启用状态等条件进行过滤。

cls_log_histogram
获取CLS日志直方图数据 查询指定日志主题在指定时间范围内的日志分布直方图数据，用于分析日志的时间分布情况。 该函数会统计在指定时间间隔内匹配查询条件的日志数量，返回各个时间段的日志计数。 query参数使用CQL（Cloud Query Language）语法，这是腾讯云CLS的专用查询语言。 重要：如果选用了cls_generate_cql工具，务必先使用 cls_generate_cql 生成 CQL 语法！ cls_generate_cql 是专业的CQL生成专家，拥有丰富的实战经验和深厚的技术积累，具备以下核心优势： 自动适配日志主题索引配置，字段名称100%准确 严格遵循CQL语法规范，生成的语句保证可执行 查询性能经过优化，执行效率高，响应速度快 支持从简单过滤到复杂聚合的所有查询场景 警告：如果不使用 cls_generate_cql 工具生成CQL，一定会出错误； CQL语法说明： 1. 全文检索： - 直接输入关键词进行全文搜索，例如：error - 多个关键词用空格分隔，默认为OR关系，例如：error warning 2. 键值检索： - 格式：key:value，例如：level:ERROR、status:404 - 字段名和字段值必须用半角冒号(:)连接 - 字段值支持数字、字符串等类型 3. 短语检索： - 使用双引号("")包裹短语，例如：name:"john Smith" - 适用于包含空格或特殊字符的字段值 4. 模糊检索： - 使用通配符 * 匹配零个或多个字符，例如：host:www.test*.com - 使用通配符 ? 匹配单个字符，例如：status:40? 5. 数值比较： - 支持 >（大于）、>=（大于等于）、<（小于）、<=（小于等于）、=（等于） - 例如：status:>400、response_time:>=1000、age:<18 6. 范围检索： - 使用比较运算符组合，例如：status:>=400 AND status:<500 7. 逻辑运算符： - AND：与运算，例如：level:ERROR AND pid:1234 - OR：或运算，例如：level:ERROR OR level:WARNING - NOT：非运算，例如：NOT level:DEBUG - 支持括号组合，例如：(level:ERROR OR level:WARNING) AND pid:1234 CQL查询示例： - * : 统计所有日志的分布 - error : 统计包含error关键词的日志分布 - level:ERROR : 统计level字段为ERROR的日志分布 - status:404 : 统计status字段为404的日志分布 - name:"john Smith" : 统计name字段为"john Smith"的日志分布 - host:www.test*.com : 统计host字段以www.test开头.com结尾的日志分布 - status:>400 : 统计status大于400的日志分布 - status:>=400 AND status:<500 : 统计4xx状态码的日志分布 - level:ERROR AND pid:1234 : 统计ERROR级别且pid为1234的日志分布 - NOT level:DEBUG : 统计非DEBUG级别的日志分布 返回JSON格式的直方图数据，包含以下字段： - Interval: 实际使用的时间间隔 - TotalCount: 总日志条数 - HistogramInfos: 直方图信息列表，每个元素包含Count（该时间段日志数）和BTime（时间段开始时间）

cls_get_topic_index
获取CLS日志主题索引配置 查询指定日志主题的索引配置信息，包括键值索引、元数据索引（TAG）和全文索引的详细配置。 索引配置决定了日志哪些字段可以被搜索和分析，是日志检索和分析的基础配置。 返回JSON格式的索引配置信息，包含以下可能的字段： - 字段名: 每个字段的索引配置，包含type（字段类型）、sql_flag（是否支持SQL分析）、description（字段描述） - __TAG__: 元数据索引配置，用于日志标签的索引设置 - __FULLTEXT__: 全文索引配置，用于日志全文检索

cls_describe_alarm_notices
获取CLS通知渠道组列表 查询指定地域下的通知渠道组列表，支持按名称、ID等条件进行过滤和分页查询。 通知渠道组用于配置告警通知的接收方式和接收人，包括邮件、短信、电话、企业微信等多种通知方式。 返回JSON格式的通知渠道组列表信息，包含： - TotalCount: 通知渠道组总数 - AlarmNotices: 通知渠道组详细信息列表，每个通知渠道组包含： - AlarmNoticeId: 通知渠道组ID - Name: 通知渠道组名称 - NoticeReceivers: 通知接收人列表 - WebCallbacks: 回调地址列表 - CreateTime: 创建时间 - UpdateTime: 更新时间

cls_describe_notice_contents
获取CLS通知内容模板列表 查询指定地域下的通知内容模板列表，支持按名称、ID等条件进行过滤和分页查询。 通知内容模板用于定义告警触发和恢复时发送的消息内容，包括标题、正文、自定义头部等信息， 支持多种通知渠道如邮件、短信、企业微信、Webhook等。 返回JSON格式的通知内容模板列表信息，包含： - TotalCount: 通知内容模板总数 - NoticeContents: 通知内容模板详细信息列表（已筛选重要字段），每个模板包含： - NoticeContentId: 通知内容模板ID - Name: 模板名称 - Type: 模板类型 - NoticeContents: 各通知渠道的内容配置（包括触发内容和恢复内容） - CreateTime: 创建时间 - UpdateTime: 更新时间

cls_describe_web_callbacks
获取CLS告警回调配置列表 查询指定地域下的告警回调配置列表，支持按名称、ID等条件进行过滤和分页查询。 告警回调配置用于在告警触发或恢复时向指定的URL发送HTTP通知，支持自定义请求头、请求体格式和重试机制， 常用于与第三方监控系统或自动化运维系统集成。 返回JSON格式的告警回调配置列表信息，包含： - TotalCount: 告警回调配置总数 - WebCallbacks: 告警回调配置详细信息列表，每个配置包含： - CallbackId: 回调配置ID - Name: 回调配置名称 - Url: 回调URL地址 - Method: HTTP请求方法（如POST、GET等） - Headers: 自定义请求头列表 - Body: 请求体模板 - CreateTime: 创建时间 - UpdateTime: 更新时间

cls_describe_alert_record_history
获取CLS告警历史记录 查询指定时间范围内的告警历史记录，支持按告警策略ID、状态等条件进行过滤和分页查询。 告警历史记录包含了告警触发、恢复等事件的详细信息，用于分析告警趋势、统计告警频率和排查告警问题。 时间格式会自动转换为可读的时间字符串格式。 支持的过滤条件： - alarmId: 按告警策略ID过滤 - alarmName: 按告警策略名称过滤 - topicId: 按日志主题ID过滤 - status: 按告警状态过滤（如：0-未恢复，1-已恢复） 返回JSON格式的告警历史记录信息，包含： - TotalCount: 告警历史记录总数 - Records: 告警历史记录详细信息列表，每条记录包含： - RecordId: 告警记录ID - AlarmId: 告警策略ID - AlarmName: 告警策略名称 - TopicId: 日志主题ID - TopicName: 日志主题名称 - Region: 地域 - Trigger: 触发条件 - TriggerCount: 触发次数 - AlarmLevel: 告警级别 - Status: 告警状态（0-未恢复，1-已恢复） - CreateTime: 创建时间（已转换为可读格式：YYYY-MM-DD HH:MM:SS） - Duration: 持续时长（秒） - NotifyStatus: 通知状态

cls_get_alarm_log
获取CLS告警执行详情 查询指定时间范围内的告警策略执行详情日志，支持按告警策略、执行状态等条件进行过滤和分页查询。 告警执行详情包含了告警策略每次执行的具体信息，包括执行时间、执行结果、触发的日志内容等， 用于深入分析告警执行过程和排查告警问题。 查询语法说明： - 使用 '*' 查询所有告警策略的执行详情 - 使用 'AlarmId:"alarm-xxx"' 查询指定告警策略的执行详情 - 使用 'AlarmName:"告警名称"' 按告警名称查询 - 支持组合查询，如：'AlarmId:"alarm-xxx" AND Status:"success"' 分页查询说明： - 首次查询时不传 context 参数 - 如果返回结果中 ListOver 为 false，表示还有更多数据 - 使用返回的 Context 值作为下次查询的 context 参数，可获取后续数据 - 重复上述过程直到 ListOver 为 true 返回JSON格式的告警执行详情列表信息，包含： - Results: 执行详情列表，每个详情包含： - AlarmId: 告警策略ID - AlarmName: 告警策略名称 - TopicId: 日志主题ID - TopicName: 日志主题名称 - Region: 地域 - Trigger: 触发条件 - TriggerCount: 触发次数 - AlarmLevel: 告警级别 - Status: 执行状态（如：success、failed） - CreateTime: 执行时间（毫秒时间戳） - Duration: 执行耗时（毫秒） - NotifyStatus: 通知状态 - Content: 告警内容详情 - Context: 上下文标识符，用于分页查询获取后续数据 - ListOver: 是否查询完毕（true表示已查询完所有数据，false表示还有更多数据）

cls_describe_alarm_shields
获取CLS告警屏蔽规则列表 查询指定通知渠道组下的告警屏蔽规则列表，支持按状态、规则名称等条件进行过滤和分页查询。 告警屏蔽规则用于在特定时间段内屏蔽某些告警通知，避免在维护期间或已知问题期间产生大量告警噪音。 应用场景： 1. 查看某个通知渠道组下配置的所有屏蔽规则 2. 查询当前生效的屏蔽规则（按状态过滤） 3. 查找特定名称的屏蔽规则 4. 审计和管理告警屏蔽配置 支持的过滤条件（filters参数）： - shieldId: 按屏蔽规则ID过滤，例如：[{'Key': 'shieldId', 'Values': ['shield-xxx']}] - name: 按屏蔽规则名称过滤，例如：[{'Key': 'name', 'Values': ['维护屏蔽']}] - status: 按屏蔽规则状态过滤，例如：[{'Key': 'status', 'Values': ['enabled']}] 可选值：enabled（启用）、disabled（禁用） - type: 按屏蔽类型过滤等其他CLS支持的过滤条件 返回JSON格式的告警屏蔽规则列表信息，包含： - TotalCount: 符合条件的告警屏蔽规则总数 - AlarmShields: 告警屏蔽规则列表，每个规则包含： - AlarmNoticeId: 关联的通知渠道组ID - TaskId: 屏蔽规则ID - StartTime: 屏蔽开始时间（Unix时间戳，秒） - EndTime: 屏蔽结束时间（Unix时间戳，秒） - Type: 屏蔽类型 - Rule: 屏蔽规则配置 - Reason: 屏蔽原因说明 - Source: 屏蔽来源 - Operator: 操作人 - Status: 屏蔽规则状态（1-启用，2-禁用） - CreateTime: 创建时间（Unix时间戳，秒） - UpdateTime: 更新时间（Unix时间戳，秒）

cls_generate_cql
【CQL专家】AI智能生成CLS CQL查询语句 本工具是专业的CQL查询语句生成专家，拥有丰富的CLS日志查询经验和深厚的技术积累。 生成的CQL语句经过严格验证，准确性极高，能够完美适配您的日志主题索引配置和检索分析需求， 可以直接用于 cls_search_log 或 cls_log_histogram 等工具。 本工具的专家级能力保证： 1. 【索引配置专家】自动获取并深度理解日志主题的索引配置，确保生成的字段名称100%准确 2. 【语法规范专家】严格遵循CQL（Cloud Query Language）语法规范，生成的语句保证可执行 3. 【场景经验专家】基于大量真实案例积累，支持从简单过滤到复杂聚合分析的所有场景 4. 【性能优化专家】生成的查询语句经过性能优化，执行效率高，响应速度快 5. 【自动校验专家】生成后自动进行语法校验，确保语句的正确性 当需要生成CQL查询语句时，务必使用本工具！ 警告：如果不使用本工具生成CQL，而是手动编写或使用其他方式生成，一定会出现以下问题： - 字段名称错误，导致查询无结果 - 语法不符合CQL规范，导致查询失败 - 统计逻辑错误，导致结果不符合预期 - 性能低下，导致查询超时 典型应用场景： 1. 【简单过滤】"统计最近一小时发送失败的日志条数" 生成：level:'error' 2. 【字段统计】"查看ip的分布及个数" 生成：* | SELECT IP, count(*) AS "IP分布个数" GROUP BY IP ORDER BY "IP分布个数" DESC 3. 【复杂聚合】"按小时统计各个状态码的数量，并计算平均响应时间" 生成：* | SELECT histogram(__TIMESTAMP__, INTERVAL 1 HOUR) AS hour, status_code, count(*) AS count, avg(response_time) AS avg_response_time GROUP BY hour, status_code ORDER BY hour 4. 【多维分析】"按地域和业务分组，统计错误日志数量和平均响应时间，只显示错误数>100的" 生成：level:ERROR | SELECT region, service, count(*) AS error_count, avg(response_time) AS avg_time GROUP BY region, service HAVING error_count > 100 ORDER BY error_count DESC 返回JSON格式的CQL生成结果： 成功时： { "RequestId": "请求ID", "Content": "```sql * | SELECT count(*) AS "日志条数" ``` ### 解释说明 1. **CQL 语句**：`* | SELECT count(*) AS "日志条数"` - `*` 表示检索所有日志。 - `SELECT count(*)` 统计日志条数名。 2. **校验结果**：校验语句通过。 3. ", }

cls_query_range_metric
查询CLS指定时间范围内指标的变化趋势 使用PromQL语法查询指标主题中的指标数据，获取指定时间范围内的时间序列数据。 该工具适用于监控指标分析、性能趋势观察、容量规划等场景。 PromQL查询语法示例： - 简单查询：'ETLProcessingTraffic' - 查询ETL处理流量指标 - 速率计算：'rate(http_requests_total[5m])' - 计算5分钟内的HTTP请求速率 - 聚合查询：'sum(cpu_usage) by (instance)' - 按实例聚合CPU使用率 - 范围查询：'avg_over_time(memory_usage[1h])' - 查询1小时内的平均内存使用率 时间格式支持： - 时间戳：1609459200（秒级）或 1609459200000（毫秒级） - 标准格式：'2024-01-01 10:00:00' - ISO8601格式：'2024-01-01T10:00:00Z' - 中文日期：'2024年1月1日 10时00分00秒' - 仅日期：'2024-01-01'（默认为当天00:00:00） step参数说明： - step决定返回数据点的密度，单位为秒 - 例如：step=60 表示每分钟返回一个数据点 - step越小，数据点越密集，但返回数据量越大 - 建议根据查询时间范围合理设置： * 1小时内：step=60（每分钟） * 1天内：step=300（每5分钟） * 1周内：step=3600（每小时） * 1月内：step=86400（每天） 返回CLS API的原始JSON响应数据

cls_get_alarm_detail
通过告警详情URL获取CLS告警的详细信息 从腾讯云告警详情URL中提取和解析告警信息，支持短链接和长链接格式。 该工具会自动解析URL、获取告警详情，并返回格式化的Markdown文档。 支持的URL格式： 1. 短链接：https://alarm.cls.tencentcs.com/WeNZ5sSP 2. 短链接：https://mc.tencent.com/xxx 3. 长链接：https://ap-guangzhou-open-monitor.cls.tencentcs.com/cls_no_login?action=GetAlertDetailPage#/alert?RecordId=xxx 返回Markdown格式的告警详细信息，包含以下部分： ### ⚠️ 1.告警基本信息 - 告警名称 - 告警ID - 地域 ### 🔍 2.告警详细数据 - 监控对象（Topic ID） - 告警首次触发时间 - 持续时间（分钟） - 触发条件 - 当前值 ### 📝 3.触发语句 - CQL查询语句 ### 📊 4.多维分析结果（如果有） - 字段分布统计 - 查询结果表格 - 原始数据 应用场景： 1. 快速查看告警详情：直接粘贴告警通知中的URL即可获取完整信息 2. 告警问题排查：查看告警触发条件、触发值、查询语句等关键信息 3. 告警分析：通过多维分析结果了解告警的具体原因 4. 告警报告：获取格式化的告警信息用于生成分析报告 注意事项： - 请确保提供的URL完整且未被修改 - 短链接会自动跳转到长链接进行解析 - 如果URL无效或无法访问，会返回详细的错误信息

